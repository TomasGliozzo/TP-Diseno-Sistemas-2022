{{#partial "contenido"}}
    <section>

            <h3 class="fw-bold text-center py-5">Editar Agente Sectorial</h3>

        <form action="{{url}}" method="post">
            <div class="mb-4">
                <label for="nombreApellido" class="form-label">Nombre y Apellido</label>
                <input type="text" class="form-control" name="nombreApellido" value="{{agente-editar.nombreApellido}}"
                       placeholder="Ingrese su nombre y apellido" required>
            </div>

            <br>

            <div class="mb-4">
                <label for="nombreUsuario" class="form-label">Usuario</label>
                <input type="text" class="form-control" name="nombreUsuario" value="{{agente-editar.usuario.nombre}}"
                       placeholder="Ingrese un nombre de usuario" required>
            </div>

            <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Guardar</button>
            </div>
        </form>

        <a class="btn btn-sm btn-outline-primary text-nowrap" href="{{urlCambio}}">Cambiar contraseña</a>
    </section>

{{/partial}}
{{>layout.hbs}}